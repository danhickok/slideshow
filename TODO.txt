Current tasks in this branch
============================

[ ] Consider moving all code into JS modules for better isolation.  Or maybe
    move those external routines to a plugin structure so we can share them.

[/] Move the project-specific styles into their own files so that we could use
    other themes if desired.

[ ] Now that we've moved the custom styles into their own file, we can use
    different themes.  It's become obvious that we need to change some of the
    custom styles to use the established CSS variables.
